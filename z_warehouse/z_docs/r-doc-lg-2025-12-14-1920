r-doc-lg-2025-12-14-1920

learning_graph {eda4mldata}	R Documentation
LearningGraph: A Knowledge Graph for Skills-Based Learning
Description
A multi-relational knowledge graph representing the data science skills ecosystem, designed for demonstrating graph theory concepts in machine learning applications.

Usage
lg_proficiency_levels

lg_competencies

lg_skills

lg_work_roles

lg_courses

lg_learners

lg_has_skill

lg_requires_skill

lg_prerequisite

lg_course_prereq

lg_teaches

lg_schema
Format
A list with the following components:

metadata
List with name, description, source, version, schema

proficiency_levels
Tibble defining the 0-4 proficiency scale

nodes
List of node tables:

competencies
7 competency areas from IC CRG

skills
18 knowledge/skills (KSAs) curated for EDA4ML scope

work_roles
3 data science job roles

courses
15 courses covering all 18 skills

learners
6 fictional learner profiles

edges
List of edge tables:

has_skill
Learner → Skill with proficiency weight (37 edges)

requires_skill
Work Role → Skill with required proficiency (25 edges)

prerequisite
Skill → Skill, conceptual dependencies (22 edges)

course_prereq
Course → Course, curricular ordering (19 edges)

teaches
Course → Skill with level taught (19 edges)

Tibble with 5 rows: proficiency levels 0-4

Tibble with 7 rows: data science competency areas

Tibble with 18 rows: curated KSAs for EDA4ML scope

Tibble with 3 rows: data science work roles

Tibble with 15 rows: courses covering all skills

Tibble with 6 rows: fictional learner profiles

Tibble with 37 rows: learner skill proficiencies

Tibble with 25 rows: role skill requirements

Tibble with 22 rows: skill prerequisite edges (DAG)

Tibble with 19 rows: course prerequisite edges (DAG)

Tibble with 19 rows: course-skill teaching relationships

Tibble with 6 rows: edge type schema (includes skill_in_cmp)

Graph Structure
The LearningGraph demonstrates several graph types:

DAG: Both skill prerequisites and course prerequisites form directed acyclic graphs (complementary views)

Bipartite: Learner-Skill and Course-Skill relationships

Weighted: Proficiency levels as edge weights

Multi-relational: Multiple edge types between node types

Proficiency Framework
Work role requirements follow typical career progression:

Entry/Mid (2-5 yrs): concentration skills at level 2, supporting at 1-2

Mid (3-5 yrs): concentration skills at 2-3, supporting at 2

Senior (5+ yrs): concentration skills at 3, supporting at 2

Level 4 (Master) is reserved for thought leaders, not job requirements

Demonstrated Algorithms
Learning paths: Find minimal skill acquisition sequence from a learner's current profile to a target skill

Gap analysis: Compare current vs. required skill profiles

Community detection: Skills cluster by competency area

Centrality: Identify foundational vs. advanced skills

Topological sort: Determine valid learning orderings

Course Structure
The 15 courses are designed to cover all 18 skills with realistic prerequisites. Entry points (no prerequisites): C01 (Programming), C03 (Probability), C04 (Linear Algebra). The course DAG has 5 levels from entry points to capstone courses (C10, C14, C15).

Course design was inspired by curricula from Georgia Tech OMSA, UC Berkeley MIDS, Stanford MS Statistics, CMU MSML, and JHU MS Data Science.

Complementary Prerequisite Views
The graph contains two prerequisite structures:

prerequisite (skill → skill): Conceptual dependencies between skills, e.g., "Linear Algebra is needed for Statistical Learning"

course_prereq (course → course): Curricular ordering, e.g., "Take C04 before C14"

These are complementary, not redundant. Courses bundle multiple skills, so course prerequisites don't derive directly from skill prerequisites.

Source
Node and edge definitions are based on the Competency Resource Guide for Data Science (2023), published by the Office of the Director of National Intelligence (ODNI). This document is UNCLASSIFIED and publicly available.

The knowledge graph structure is inspired by Workera.ai's skills intelligence platform, which provides AI-powered skill assessment and personalized learning paths for enterprise workforces.

Example Usage
library(igraph)
library(dplyr)

# Load the data
data(learning_graph)

# Build skill prerequisite DAG
prereq_edges <- learning_graph$edges$prerequisite |>
  left_join(learning_graph$nodes$skills, by = c("skill_from_id" = "skill_id")) |>
  rename(from = skill_name) |>
  left_join(learning_graph$nodes$skills, by = c("skill_to_id" = "skill_id")) |>
  rename(to = skill_name) |>
  select(from, to)

g_skills <- graph_from_data_frame(prereq_edges, directed = TRUE)
plot(g_skills, layout = layout_with_sugiyama(g_skills)$layout)

# Build course prerequisite DAG
course_edges <- learning_graph$edges$course_prereq |>
  left_join(learning_graph$nodes$courses, by = c("course_from_id" = "course_id")) |>
  rename(from = course_tag) |>
  left_join(learning_graph$nodes$courses, by = c("course_to_id" = "course_id")) |>
  rename(to = course_tag) |>
  select(from, to)

g_courses <- graph_from_data_frame(course_edges, directed = TRUE)
plot(g_courses, layout = layout_with_sugiyama(g_courses)$layout)
References
Office of the Director of National Intelligence. (2023). Competency Resource Guide for Data Science. https://www.dni.gov/

Workera.ai. (2024). Skills Intelligence Platform. https://workera.ai/

See Also
lg_skills for the skills table alone, lg_courses for the expanded course list, lg_prerequisite for the skill prerequisite edge list, lg_course_prereq for the course prerequisite edge list

Examples
Run examples

# Summary of the knowledge graph
sapply(learning_graph$nodes, nrow)
sapply(learning_graph$edges, nrow)

# View courses
learning_graph$nodes$courses

# View course prerequisites
learning_graph$edges$course_prereq

# View proficiency levels
learning_graph$proficiency_levels

[Package eda4mldata version 0.1.0 Index]
