learning-path-demo-warning-2025-12-13-1545

```{r}
#| label: learning-path-demo

path <- shortest_paths(g_prereq, 
                       from = "Programming", 
                       to = "Statistical Learning",
                       output = "vpath")
cat(paste(names(path$vpath[[1]]), collapse = " → "))
```

Warning in shortest_paths(g_prereq, from = "Programming", to = "Statistical Learning",  :
  At vendor/cigraph/src/paths/unweighted.c:444 : Couldn't reach some vertices.
